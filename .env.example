# Required for the diagnostic enquiry form (Formspree or compatible JSON endpoint).
# Example: https://formspree.io/f/yourFormId
# For GitHub Pages, add the same value as repository secret VITE_FORM_ENDPOINT
# (Settings → Secrets and variables → Actions).
VITE_FORM_ENDPOINT=
